# Database connection string
DATABASE_URL=postgresql://localhost/gitpub

# JWT secret for token signing (minimum 32 bytes)
# Generate with: openssl rand -base64 32
JWT_SECRET=REPLACE_WITH_GENERATED_SECRET_FROM_OPENSSL_RAND_BASE64_32
